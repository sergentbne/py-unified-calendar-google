[project]
name = "py-unified-calendar"
version = "0.1.0"
description = "Generic calendar library for simple handling of different calendar services"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "geopy>=2.5.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
